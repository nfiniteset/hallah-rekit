input GuestInput {
  name: String!
}

type Guest {
  id: ID!,
  name: String!,
  dietaryRestrictions: [DietaryRestriction]!
}

type DietaryRestriction {
  label: String!
}

type Query {
  guests: [Guest]!
  guest(id: ID!): Guest!
}

type Mutation {
  createGuest(input: GuestInput!): Guest
}
